query ListProjectMembershipsQuery($ownerName: String!, $repositoryName: String!) {
    repositoryEntries: repository(owner: $ownerName, name: $repositoryName) {
        collaborators {
            nodes {
                databaseId
                login
                email
            }
        }
    }
}
