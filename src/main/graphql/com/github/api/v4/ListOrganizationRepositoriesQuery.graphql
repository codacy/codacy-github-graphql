query ListOrganizationRepositoriesQuery(
  $organizationName: String!
  $size: Int = 100
  $page: String
) {
  organization(login: $organizationName) {
    repositories(
      first: $size
      after: $page
    ) {
      nodes {
        ... on Repository {
          databaseId
          name
          nameWithOwner
          url
          sshUrl
          isPrivate
          viewerPermission
          updatedAt
          pushedAt
        }
      }
      pageInfo {
        endCursor
      }
    }
  }
}
