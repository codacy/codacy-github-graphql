query ListProjectsThinQuery(
  $organizationName: String!
  $size: Int = 100
  $page: String
) {
  organization(login: $organizationName) {
    repositories(
      first: $size
      after: $page
      orderBy: { field: PUSHED_AT, direction: DESC }
    ) {
      nodes {
        ... on Repository {
          databaseId
          url
          sshUrl
          pushedAt
          viewerPermission
          isPrivate
        }
      }
      pageInfo {
        endCursor
      }
    }
  }
}
